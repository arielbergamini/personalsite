version: 1
frontend:
    phases:
        preBuild:
            commands:
                - npm install -g sass
        build:
            commands:
                - sass assets/scss/style.scss assets/css/style.css
    artifacts:
        baseDirectory: /
        files:
            - '**/*'
    cache:
        paths:
            - []
