version: 1
frontend:
    phases:
        preBuild:
            commands:
                - npm install bootstrap
                - npm install -g sass
        build:
            commands:
                - sass assets/scss/custom.scss assets/css/custom.css
    artifacts:
        baseDirectory: /
        files:
            - '**/*'
    cache:
        paths:
            - node_modules/**/*